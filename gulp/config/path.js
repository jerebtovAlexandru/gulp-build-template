// Импорт системного модуля Node.js 'path' для корректной работы с путями файловой системы независимо от ОС
import * as nodePath from "path";

// Автоматическое определение имени корневой папки вашего проекта (используется, например, для названия ZIP-архива)
const rootFolder = nodePath.basename(nodePath.resolve());

// Определение имени и пути к папке с готовой сборкой (дистрибутивом)
const buildFolder = `./dist`;
// Определение имени и пути к папке с исходными файлами разработки
const srcFolder = `./src`;

// Экспорт единого объекта path, содержащего все необходимые траектории для задач Gulp
export const path = {
  // Пути, куда Gulp будет сохранять скомпилированные и обработанные файлы (папка назначения)
  build: {
    js: `${buildFolder}/js/`, // Папка для готовых JavaScript скриптов
    css: `${buildFolder}/css/`, // Папка для скомпилированных стилей CSS
    html: `${buildFolder}/`, // Корневая папка сборки для готовых HTML страниц
    images: `${buildFolder}/img/`, // Папка для оптимизированных изображений
    files: `${buildFolder}/files/`, // Папка для скопированных статичных файлов
    fonts: `${buildFolder}/fonts/`, // Папка для сконвертированных веб-шрифтов
  },
  // Пути к исходным файлам проекта, которые Gulp должен взять в работу
  src: {
    // Маска для поиска всех растровых картинок указанных расширений во всех подпапках директории img
    images: `${srcFolder}/img/**/*.{jpg,jpeg,png,gif,webp}`,
    // Маска для поиска векторных SVG картинок в исходниках
    svg: `${srcFolder}/img/**/*.svg`,
    // Главная точка входа для сборщика JavaScript (Webpack)
    js: `${srcFolder}/js/app.js`,
    // Главный файл стилей SCSS, в который импортируются все остальные компоненты стилей
    scss: `${srcFolder}/scss/style.scss`,
    // Маска для поиска всех HTML файлов в корне папки исходников (не трогая файлы в подпапках, например, в components/)
    html: `${srcFolder}/*.html`,
    // Маска для поиска абсолютно любых статичных файлов во всех вложенных директориях папки files
    // ** — любые вложенные папки, *.* — любые файлы с любым расширением
    files: `${srcFolder}/files/**/*.*`,
    // Папка со всеми отдельными SVG иконками, из которых будет собираться общий спрайт
    svgicons: `${srcFolder}/svgicons/*.svg`,
    // Маска для поиска исходных файлов шрифтов для последующей конвертации
    fonts: `${srcFolder}/fonts/**/*.*`,
  },
  // Пути к файлам, за которыми Gulp-наблюдатель (watcher) будет следить в реальном времени для перезапуска тасков
  watch: {
    // Отслеживание изменений во всех картинках (включая вектор и иконки favicon) для их обновления в сборке
    images: `${srcFolder}/img/**/*.{jpg,jpeg,png,gif,webp,svg,ico}`,
    // Слежение за абсолютно любым JS-файлом во всех подпапках скриптов
    js: `${srcFolder}/**/*.js`,
    // Слежение за абсолютно любым SCSS-файлом во всех подпапках стилей
    scss: `${srcFolder}/**/*.scss`,
    // Слежение за абсолютно любым HTML-файлом во всех подпапках (включая части/компоненты страниц)
    html: `${srcFolder}/**/*.html`,
    // Слежение за изменениями в папке статичных файлов
    files: `${srcFolder}/files/**/*.*`,
  },
  // Указание целевой папки, которую нужно полностью удалять перед началом новой сборки (метод reset)
  clean: buildFolder,
  // Дублирование путей в корневые свойства объекта для быстрого доступа из других тасков
  buildFolder: buildFolder,
  srcFolder: srcFolder,
  rootFolder: rootFolder,
  // Название папки на удаленном сервере, в которую будет осуществляться FTP-деплой
  ftp: `test`,
};
